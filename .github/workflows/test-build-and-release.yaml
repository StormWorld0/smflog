name: Maturin Build & Release

on:
  push:
    branches: [ "main" ]
    tags:
      - 'v*'
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Compilation & Artifact Generation (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
          
      - name: Rust Cache Manager
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.os }}-pep517

      - name: Install PEP 517 Build Frontend
        run: python -m pip install --upgrade pip build

      # Generate sdist (.tar.gz) and wheels (.whl)
      - name: Build via pyproject.toml
        run: python -m build

      # Export artifacts from container runner
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: pypi-artifacts-${{ matrix.os }}
          path: dist/*

  publish:
    name: Publish to PyPI
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    
    permissions:
      id-token: write 
      contents: read
      
    # Binding execution to the GitHub environment
    environment:
      name: pypi
      url: https://pypi.org/p/smflog

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: pypi-artifacts-*
          merge-multiple: true

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        
